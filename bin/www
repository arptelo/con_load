#!/usr/bin/env node

var appConfig = require('./../server');
var app = appConfig.app;
var config = appConfig.config;
app.listen(config.port, function(){
    console.log("Listening on port " + config.port);
});